// la defizione di un  tipo di dato custom, si puo fare
//type, interface, class
export type Car = {
    nome: string;
    modello: string;
}

export interface ICcar  {
    nome: string;
    modello: string;
}
